apiVersion: v1
kind: Service
metadata:
  name: auth-gateway
  namespace: auth-gateway
  labels:
    app: auth-gateway
spec:
  type: NodePort
  selector:
    app: auth-gateway
  ports:
    - name: http
      port: 8090
      targetPort: http
      nodePort: 31091
