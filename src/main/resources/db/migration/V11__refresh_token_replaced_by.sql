-- 幂等重投递：记录 refresh token 旋转后的「后继令牌」哈希（前驱→后继正向链接）。
-- 用途：一个已旋转的 token 被再次提交且超出旋转宽限期时，据后继是否已被消费来区分
--   * 后继从未被使用 → 客户端从未收到旋转响应（如笔记本合盖休眠、响应在途丢失）→ 合法重投递，不灭族；
--   * 后继已被消费（链已前进） → 真正的重放/分叉 → 维持灭族。
-- NULL 表示旧数据或首次登录令牌尚未旋转；不回填。
ALTER TABLE refresh_tokens ADD COLUMN replaced_by_hash VARCHAR(64);
